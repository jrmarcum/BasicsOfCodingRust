##### Run Command:

`$ cargo run`

##### Results:

`2`
`false`
`true`
`false`
`[peach apple pear]`
`[PEACH APPLE PEAR PLUM]`
___
#### In idiomatic Rust these collection operations are built-in iterator methods (`.iter().filter(...)`, `.iter().map(...)`, `.iter().any(...)`, `.iter().all(...)`); the standalone functions here mirror the Go example structure.

Running our program prints the sorted string and int slices and `true` as the result of our sorted check.
___
##### Run Command:

`$ cargo run`

##### Results:

`Strings: [a b c]`
`Ints:    [2 4 7]`
`Sorted:  true`

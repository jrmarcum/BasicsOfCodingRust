##### Run Command:

`$ cargo run`

##### Results:

`1+2 = 3`
`1+2+3 = 6`
___
#### There are several other features to Rust functions. One is multiple return values via tuples, which we'll look at next.

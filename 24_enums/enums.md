Rust has first-class `enum` types, whereas Go simulates enums with `iota` integer constants. Rust's `match` expression handles enum variants exhaustively. The `Display` trait replaces Go's `fmt.Stringer` interface for string conversion.
___
##### Run Command:

`$ cargo run`

##### Results:

`connected`
`idle`

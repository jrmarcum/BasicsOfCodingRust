##### Run Command:

`$ cargo run`

##### Results:

`7 is odd`
`8 is divisible by 4`
`9 has 1 digit`
___
#### There is no [ternary if](http://en.wikipedia.org/wiki/%3F:) in Rust, so you'll need to use a full `if` statement even for basic conditions.
